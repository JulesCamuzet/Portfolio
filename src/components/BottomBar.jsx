import { styled } from "styled-components"

const _BottomBar = styled.div`
        width: 100%;
        height: 10vh;
    `

const BottomBar = () => {

    return (
        <_BottomBar />
    )
} 

export default BottomBar;
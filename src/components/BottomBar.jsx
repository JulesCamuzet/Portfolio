import { styled } from "styled-components"

const BottomBar = () => {
    const BottomBar = styled.div`
        width: 100%;
        height: 10vh;
    `

    return (
        <BottomBar />
    )
} 

export default BottomBar;